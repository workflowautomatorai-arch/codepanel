# Skips requests to AI, will return mocked responses from backend
IS_MOCK=false

# API base URL (your self-hosted backend)
VITE_API_BASE_URL=http://localhost:3000

# Use self-hosted backend, no auth
VITE_SELF_HOSTED_MODE=true
